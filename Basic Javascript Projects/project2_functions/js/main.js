function displaydate ()  { // this is the function
    var str = "this is the button text"; // this is the name of the variable
document.getElementById("Date") .innerHTML = Date ();// this is the function that calls the i.d. date
}

function Myoperator() {
        var sentence = "this is an "; //the variable
        sentence += "operator assignment"; // this is the operator to concatenate the string 
        document.getElementById("operator").innerHTML=sentence;// this is the method to display the variable
}